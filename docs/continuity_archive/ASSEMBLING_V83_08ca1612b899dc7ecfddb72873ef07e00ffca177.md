# Work Continuity — Matrix Assembling

Last updated: 2026-09-07  
Repository: `MATRIXNEO23/assembling`  
Canonical branch: `main`  
Active supervisor branch: `foundation-closure-f1-f2`  
Continuity schema: `matrix.assembling.continuity.v83`

## Complete preserved history

Previous complete continuities are preserved byte-for-byte:

- `docs/continuity_archive/ASSEMBLING_V80_e1c495b5f00530e4d3216785849496d709a4fdcc.md`
- `docs/continuity_archive/ASSEMBLING_V81_7638c5b4f09095dff973f7fb88ace98c12e995fb.md`
- `docs/continuity_archive/ASSEMBLING_V82_ff92e9dc6839dd6d3da7b06af0977658174e82ed.md`

The v82 archive preserves the full dependency-ordered Phase A-F roadmap. This v83 file is the current operational handoff and records the latest method correction and feedback preservation without deleting any previous plan item.

---

# 1 — CURRENT EXECUTION STATE

```text
THIS CHAT = supervisor / architecture / audit / evidence / continuity
CHATGPT WORK = active executor for Student-5 Path B
Do not duplicate Work execution in this chat.
```

Active Work:

```text
repo = MATRIXNEO23/matrix-understanding-lab
branch = student5-path-b-v3
handoff checkpoint = aca5968bc26f8d4d4ef22327f0d225b9685e1474
assignment = prompts/WORK_STUDENT_5_PATH_B_V3_COMPLETION.md
TASK 2.3 = ACCEPTED / DO NOT REPEAT
current expected gate = TASK 2.4 physical V3 BERT/ONNX closure
next on PASS = Path-B Matrix-NLU V3 training/error-mining according to assignment gates
```

Owner reported Work started. No claim about later Work progress is valid until repository-visible evidence is inspected.

Student roles remain:

```text
Student-5 Path B = ACTIVE primary Matrix-NLU V3 candidate
Student-5 pristine 40k = immutable Path-B base
Student-5 Path A = REFERENCE untaught INT8 runtime probe
Student-4 v2.2A = REFERENCE comparative/regression baseline
```

Assembling remains waiting:

```text
repo = MATRIXNEO23/assembling
branch = foundation-closure-f1-f2
PR #23 = DRAFT / NOT MERGED
F1/F2 structured wiring = verified only for tested structured cases
real language comprehension = NOT YET DEMONSTRATED
Memory = NOT IMPLEMENTED / NOT INTEGRATED
full engine = NOT DEMONSTRATED
phone = BLOCKED
```

---

# 2 — METHOD CORRECTION: OPERATIONAL STATE MUST BE VERIFIED BEFORE ANSWERING

Owner identified a supervisor failure: the assistant confused the repository being used by Work with the repository being supervised in chat.

Root cause:

```text
operational-state answer was produced from conversational memory
instead of first reading the canonical repository/workstream state
```

Canonical correction is now persisted in:

```text
docs/MATRIX_ENGINE_WORK_METHOD.md
commit = 4942d57b0b3b4104f43752b7fa0ca40e6ee76cb7
```

New binding rule:

```text
OPERATIONAL CLAIM
-> SOURCE-OF-TRUTH READ
-> REPO/BRANCH/HEAD/TASK VERIFICATION
-> ANSWER
```

Before answering questions about repository, branch, HEAD, task, executor, Work status, gate, artifact or next operational step, the supervisor must:

```text
1. identify the relevant repository/workstream;
2. read its canonical continuity;
3. verify real branch/HEAD when current state matters;
4. distinguish Work repo/branch from supervisor repo/branch;
5. distinguish executed work from prepared/authorized work;
6. prefer repository/continuity evidence over partial conversational recollection;
7. state UNKNOWN/NOT_VERIFIED instead of guessing.
```

It is forbidden to answer first and verify afterwards for operational-state questions.

---

# 3 — TEST METHOD EXTENSION: INPUT -> MIP -> HANDOFF -> OUTPUT

The canonical work method was also extended so real module tests are owner-verifiable at each boundary.

Required trace:

```text
REAL INPUT
-> REAL PRODUCER/MODULE
-> ACTUAL OUTPUT
-> EXPECTED OUTPUT
-> FIELD-BY-FIELD DIFF
-> MIP/CONTRACT REPRESENTATION
-> ADAPTER TRANSFORMATION, IF ANY
-> ACTUAL INPUT DELIVERED TO NEXT CONSUMER
-> ACTUAL NEXT-CONSUMER OUTPUT
-> FIRST DIVERGENCE
```

Every tested boundary must record:

```text
fieldsPreserved
fieldsChanged
fieldsLost
fieldsInvented
identity/provenance preservation
explicit uncertainty/error-state preservation
reasonCodes
confidence/sourceReliability where applicable
```

If the next real consumer is not implemented/integrated, the proof stops at that boundary and the verdict remains `PARZIALMENTE DIMOSTRATO` / `NON ANCORA DIMOSTRATO` for the larger chain.

This extends, not replaces, the existing canonical rules for atomic tests, cross-module tests, E2E and DiagnosticTrace.

---

# 4 — PRODUCT FEEDBACK FILE CREATED

Persistent feedback file:

```text
docs/PRODUCT_FEEDBACK_IDEAS.md
commit = e61f6870854946e3512aa984ccaf077d2d7fa25f
status = DRAFT / NOT YET SENT
```

Preserved feedback items:

```text
FEEDBACK-001
Privacy-preserving reuse of abstract/generalizable methods learned from successful interactions,
without transferring users' personal data, identities, private conversation content or conversation-specific memories.

FEEDBACK-002
True concurrent voice conversation + operational work,
so the user can keep talking, correcting and adding constraints while tools/Work execute,
without having to close voice mode and without losing task continuity.
```

These feedback items are outside the Matrix Engine execution backlog and do not displace project priorities. They will be refined into concise submission text when the owner asks to prepare/send feedback.

---

# 5 — MASTER EXECUTION ORDER REMAINS UNCHANGED

The full canonical Phase A-F roadmap remains preserved in v82 and is still binding.

Immediate dependency order remains:

```text
A1 Student-5 TASK 2.4 physical V3 BERT/ONNX gate
-> A2 Path-B training/error-mining
-> A3 taught FP32 lock
-> A4 taught ONNX + quantization/parity
-> A5 final Frozen evaluation after tuning lock
-> A6 supervisor acceptance
-> Phase B real raw-text Student-5 -> Understanding V3 -> Context/Retrieval/Coherence -> Authority proof
-> Phase C Belief/Authority + Memory foundation
-> later cognitive state owners
-> realization/GGUF/validation/consolidation/world loop
-> full E2E
-> Android
-> Moto G56
```

New requirements are inserted according to dependency, never by recency. Nothing from v82 is silently removed.

---

# 6 — CURRENT GUARDS

```text
WORK ACTIVE = Student-5 Path B
DO NOT duplicate Work task in supervisor chat
VERIFY SOURCE OF TRUTH BEFORE ANY OPERATIONAL-STATE ANSWER
DO NOT merge PR #23 without explicit owner approval
DO NOT modify Student-4 baseline
DO NOT modify Student-5 Path A or pristine
DO NOT overwrite any model/data/artifact version
DO NOT integrate Student-5 into Assembling before lab lock + supervisor acceptance
EVERY REAL TEST MUST SHOW INPUT -> MIP -> HANDOFF -> OUTPUT
RECORD LOST / CHANGED / INVENTED FIELDS + FIRST DIVERGENCE
DO NOT call fixture-only work functional
Memory remains NOT IMPLEMENTED
PHONE TEST remains BLOCKED
```

## Exact supervisor resume action

When the owner returns with a Work update:

```text
1. read MATRIXNEO23/matrix-understanding-lab/docs/WORK_CONTINUITY_STUDENT_5.md
2. verify branch student5-path-b-v3 and actual HEAD
3. inspect new reports/artifact registry/persistent releases/checksums
4. verify results against prompts/WORK_STUDENT_5_PATH_B_V3_COMPLETION.md
5. update Phase A statuses in the canonical continuity
6. do not rely only on Work's prose summary
```
